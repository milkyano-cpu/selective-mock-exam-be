# Copy file ini menjadi docker-compose.override.yml untuk development lokal
services:
  api:
    build:
      target: development
    environment:
      NODE_ENV: development
    volumes:
      - .:/app
      - /app/node_modules
